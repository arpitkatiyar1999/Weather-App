package com.inscroller.weatherapp.utils

enum class TemperatureUnits(val value: String) {
    CELSIUS("c"),
    FAHRENHEIT("f")
}

enum class WindUnits(val value: String) {
    MPH("mph"),
    KPH("kph")
}